[View]
interpolation = gaussian #none, nearest, bilinear, bicubic, spline16, spline36, hanning, hamming, hermite, kaiser, quadric, catrom, gaussian, bessel, mitchell, sinc, lanczos

[Accuracy]
emissivity = 0.95

[Temperature_Range]
range_enable = False
range_min = -40
range_max = 300

[Monitor]
monitor_pixels_enable = False #Monitor pixels, if they are in tolerance. If not, turn screen red.
monitor_buzzer_enable = True #Buzz once if monitored pixels are not in temperature tolerance.
monitor_pixels_array = [[0, 0, 20, 50], [0, 31, 30, 40], [23, 0, 10, 20], [23, 31, -10, 0], [11, 15, -40, 300]] #[[pos_y, pos_x, min_°C, max_°C], [...], ...]
monitor_autotrigger_enable = False #A picture will be taken, if all pixels of 'monitor_autotrigger_array' are in temperature tolerance.
monitor_autotrigger_previous_frame = 5 #If a picture should automatically be taken, store a previous frame. For the current frame, set to '0'.
monitor_autotrigger_array = [[0, 0, 10, 50], [0, 31, 10, 50], [23, 0, 35, 50], [23, 31, 10, 50], [11, 15, 10, 50]] #[[pos_y, pos_x, min_°C, max_°C], [...], ...]

[Save]
save_path = /home/pi/thcam/saves/default
save_format = png
save_prefix = THC_
save_suffix = 

